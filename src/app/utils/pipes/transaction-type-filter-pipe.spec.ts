import { TransactionTypeFilterPipe } from './transaction-type-filter-pipe';

describe('TransactionTypeFilterPipe', () => {
  it('should create an instance', () => {
    expect(new TransactionTypeFilterPipe()).toBeTruthy();
  });
});
