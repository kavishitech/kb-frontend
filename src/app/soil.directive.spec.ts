import { SoilDirective } from './soil.directive';

describe('SoilDirective', () => {
  it('should create an instance', () => {
    const directive = new SoilDirective();
    expect(directive).toBeTruthy();
  });
});
